{
  "name": "phonebook-backend",
  "version": "0.1.0",
  "description": "Backend for phonebook app",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build:ui": "rm -fr build && cd ../../part02/phonebook && npm run build && cp -r build ../../part03/phonebook-backend",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Álvaro Costa",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "morgan": "^1.10.0"
  }
}
